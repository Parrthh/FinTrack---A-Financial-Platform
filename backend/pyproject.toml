[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["alembic/versions", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "B", "UP"]
# B008: FastAPI's Depends() in defaults is the framework's intended pattern.
ignore = ["B008"]

[tool.pytest.ini_options]
testpaths = ["tests"]
